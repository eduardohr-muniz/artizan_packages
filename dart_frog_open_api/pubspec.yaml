name: dart_frog_open_api
description: OpenAPI/Swagger spec generator for Dart Frog — file-system based
  route scanning.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  archive: ^4.0.0
  dart_frog: ^1.1.0
  zto:
    path: ../zto

dev_dependencies:
  lints: ^6.0.0
  mocktail: ^1.0.3
  test: ^1.25.6
